program test;
    var n: integer;

    integer Fibonacci(integer n);
        var tmp: integer;
        begin
            if (n<3) then
                return 1;

            tmp = Fibonacci(n - 2) + Fibonacci(n - 1);
            return tmp
        end;

    begin
        read(n);
        Fibonacci(n)
    end.